# dsh-anchor 实验记录

> 原则：没实测不声称。每个能力声明带实验编号与对照组；本记录按原始数据核对。

## EXP-1 锚点树协议

会话锚点 = 预承诺 + 即时对账。原语四件套：open（intent+pre 预承诺）/ close（post 对账判词 OK|DIVERGED）/ recover（扫树三证据）/ adopt（收养续跑）。锚点树目录即会话真相源：崩溃 → 扫树 → 收养 → 继续。

## EXP-2 采样策略（Lucas 高熵锁定）

三种子×七策略×200 轮×200 步对照判决：

- 捕获率：Lucas 系 100% vs 均匀 10/20 步 98%/87%。
- 成本：Lucas 高熵锁定+最小间隔 7——每捕获成本 25.1（均匀 5 步 40 的 63%）。
- 延迟：27.6 步（κ 自适应 45.4 的快 39%）——14% 额外开销换 39% 延迟改进。
- κ 自适应（TCP 式）弱点：收敛慢（稳定期锚点稀、翻车初期漏抓）。混合策略为后续候选。

## EXP-3 四测试验收（37 断言）

| 测试 | 断言 |
|---|---|
| P1 采样策略 | 10 |
| P2 插件挂载 | 12 |
| P3 跨进程收养 | 7 |
| P4 双盲外部污染 | 8 |

P4 双盲：独立进程注入外部污染（插件不知情），注入的外部污染 100% 被当场 DIVERGED 捕获。

## EXP-4 P4 双盲握手修复（CI 竞态）

Windows CI 上 P4 稳定红（重跑复现，每次漏捕获位置不同）：注入窗口原为"发信号 → sleep 50ms → 对账"，注入进程是 5ms 轮询的独立进程——CI runner 调度延迟把"检测信号+写污染"拖出 50ms 窗口，污染落在对账之后（本机调度快从未暴露）。

修复：时间窗口换成**文件系统握手**——注入进程每步必写完成确认（`.polluted-i` / `.skipped-i`），driver 等确认出现才 post，注入因果由文件系统强制保证，时间只做兜底上限。CI 连绿两跑验证。

判决：跨进程测试的正确性不能押在时间窗口上，握手/信号文件才是确定性协议；本机全绿 ≠ CI 全绿（调度延迟差异是隐形变量）。

## EXP-5 Linux 实测

锚点树协议在 Linux 上 37/37（dsh-cross-platform 实验环境，零代码改动）——协议本身平台无关（Node 文件系统 + 时间）。macOS 未实测，未实测前不声称。

## EXP-6 实现经验清单

① 恒等对账：intended/observed 必须同一命名空间——open 写 expect，close 只做比较。② 相位窗口：所有写入 tmp+rename 原子，不留半写文件（半写=脏收养源）。③ 空锚点目录容错：recover 对空目录/缺文件全部降级容错。④ 并发 rename 竞争：跨进程竞争败者静默。⑤ Windows 路径清洗：动作名非法字符替换。⑥ 幂等：close 重复调用安全；adopt 续跑幂等。
